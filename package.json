{
  "name": "nextjs-graphql",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "dev": "yarn workspace client dev",
    "build": "yarn workspace client build",
    "start": "yarn workspace client start"
  },
  "dependencies": {
    "next": "10.0.4",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
